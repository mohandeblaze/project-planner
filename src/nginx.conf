worker_processes 1;

events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        server_name axiomarc.com;

        location / {
            proxy_pass http://app:80;  # Assuming your .NET Core app is running on the "app" service on port 80
        }
    }
}
