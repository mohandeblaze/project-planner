import { PullRequestType, TaskType } from '@project-planner/shared-schema'

export const PullRequestBranches: PullRequestType[] = ['dev', 'master', 'beta']

export const TaskTypes: TaskType[] = ['main', 'test']
