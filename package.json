{
    "name": "project-planner-main",
    "scripts": {
        "install-lib": "cd packages/schema/ && npm install",
        "install-client": "npm run lib && cd client && npm install",
        "install-server": "npm run lib && cd server && npm install",
        "build-client": "cd client && npm run build",
        "build-server": "cd server && npm run build",
        "start-client": "cd client && npm start",
        "start-server": "cd server && npm start"
    }
}
